#include <stdio.h>

void main() {

   printf("Required memory size is %d",sizeof(int)); 
   printf("\nRequired memory size is %d",sizeof(short int));
   printf("\nRequired memory size is %d",sizeof(long int));
   printf("\nRequired memory size is %d",sizeof(char));
   printf("\nRequired memory size is %d",sizeof(float));
   printf("\nRequired memory size is %d",sizeof(double));
   printf("\nRequired memory size is %d",sizeof(long double));
   
}
